// DEPRECATED: Not required for the project

/**
 * All code here will be implemented to decode a given IEEE-754 2008 binary string to its decimal representation.
 */

/**
 * This function will be called by the viewController when the user activates the decoder.
 * @param {*} binaryInput The given binary string that should be converted to decimal 64.
 * @returns An object containing the steps taken and the final answer.
 */
function decodeDecimal64(binaryInput) {
  const steps = [];
  let finalAnswer = null;

  // TODO: Implement this.

  // Returns the string array of steps taken that will be displayed to the user. This is expected to be a list of strings.
  // Returns also the final answer -- this is expected to be of type Number.
  return { steps, finalAnswer };
}
